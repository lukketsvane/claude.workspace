name: Astra coding-agent assignment probe
on:
  push:
    branches: [main]
permissions:
  contents: read
  issues: write
  copilot-requests: write
jobs:
  assign:
    if: contains(github.event.head_commit.message, 'Retry Astra coding-agent assignment with Copilot permission')
    runs-on: ubuntu-latest
    steps:
      - name: Assign issue 6 to Copilot with GPT-6 Astra
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          curl --fail-with-body -sS -X POST
          -H "Accept: application/vnd.github+json"
          -H "X-GitHub-Api-Version: 2022-11-28"
          -H "Authorization: Bearer $GH_TOKEN"
          https://api.github.com/repos/lukketsvane/claude.workspace/issues/6/assignees
          -d '{"assignees":["copilot-swe-agent[bot]"],"agent_assignment":{"target_repo":"lukketsvane/claude.workspace","base_branch":"main","custom_instructions":"Use exactly GPT-6 Astra. Complete only the issue task. Do not modify unrelated files.","custom_agent":"","model":"gpt-6-astra"}}'
